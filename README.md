# require-pr-test-plan
Fails the pull request check unless the PR body includes a markdown heading named Test plan (any ## level, case-insensitive). Empty bodies fail; heading alone passes. Re-runs when the description is edited so authors can fix it without a new commit. JavaScript Action for GitHub Marketplace — no checkout, no repo contents, no extra config.
